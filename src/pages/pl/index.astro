---
import { getCollection } from 'astro:content';
import Base from '../../layouts/Base.astro';

const articles = (await getCollection('articlesPl'))
  .filter(a => !a.data.draft)
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf());
---

<Base title="ACTA FRAGILIA" lang="pl">

  <header class="hero">
    <div class="hero-grid" aria-hidden="true">
      <svg xmlns="http://www.w3.org/2000/svg" preserveAspectRatio="none">
        <defs>
          <pattern id="grid" width="60" height="60" patternUnits="userSpaceOnUse">
            <path d="M 60 0 L 0 0 0 60" fill="none" stroke="var(--line-strong)" stroke-width="0.8"/>
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#grid)" />
        <line x1="0" y1="0" x2="100%" y2="100%" stroke="var(--accent)" stroke-width="0.5" opacity="0.15"/>
        <line x1="100%" y1="0" x2="0" y2="100%" stroke="var(--accent)" stroke-width="0.5" opacity="0.15"/>
      </svg>
    </div>
    <div class="hero-content">
      <p class="hero-eyebrow">NiezaleЕјne dziennikarstwo i analiza</p>
      <h1>ACTA <em>FRAGILIA</em></h1>
      <p class="hero-lead">Prawo, ekonomia, konflikty korporacyjne i procesy spoЕ‚eczne вЂ” bez szumu, z weryfikacjД… faktГіw i uwagД… na konsekwencje.</p>
      <div class="hero-meta">
        <span><span class="dot" aria-hidden="true"></span> DЕ‚ugie teksty</span>
        <span><span class="dot" aria-hidden="true"></span> Dokumenty</span>
        <span><span class="dot" aria-hidden="true"></span> Ељledztwa</span>
      </div>
      <a href="#latest" class="hero-cta">Czytaj materiaЕ‚y в†’</a>
    </div>
  </header>

  <section id="latest">
    <div class="section-header">
      <h2>Najnowsze publikacje</h2>
      <span class="line" aria-hidden="true"></span>
    </div>

    <div class="articles-list">
      {articles.map(article => {
        const d = new Date(article.data.date);
        const dateStr = d.toLocaleDateString('pl-PL', { day: '2-digit', month: '2-digit', year: 'numeric' });
        return (
          <article class="article-card">
            <div class="article-marginalia">
              <a href={`/pl/archive?category=${article.data.category}`} class="article-category">{article.data.category}</a>
              <time class="article-date" datetime={d.toISOString().split('T')[0]}>{dateStr}</time>
            </div>
            <div class="article-body">
              <h3><a href={`/pl/article/${article.slug}`}>{article.data.title}</a></h3>
              <p>{article.data.subtitle}</p>
              <a href={`/pl/article/${article.slug}`} class="article-link">Czytaj w caЕ‚oЕ›ci в†’</a>
            </div>
          </article>
        );
      })}
    </div>
  </section>

  <section>
    <div class="section-header">
      <h2>PrzeglД…daj wedЕ‚ug tematu</h2>
      <span class="line" aria-hidden="true"></span>
    </div>
    <ul class="category-list home-categories">
      <li><a href="/pl/archive?category=Prawo">Prawo</a> вЂ” Analiza ustawodawstwa, praktyki sД…dowej i kolizji prawnych.</li>
      <li><a href="/pl/archive?category=Ekonomia">Ekonomia</a> вЂ” Analiza konfliktГіw korporacyjnych, upadЕ‚oЕ›ci, mechanizmГіw podatkowych i finansowych.</li>
      <li><a href="/pl/archive?category=SpoЕ‚eczeЕ„stwo">SpoЕ‚eczeЕ„stwo</a> вЂ” Procesy spoЕ‚eczne, instytucje i ich transformacja.</li>
      <li><a href="/pl/archive?category=Media">Media</a> вЂ” PrzepЕ‚ywy informacyjne, dziennikarstwo i umiejД™tnoЕ›Д‡ korzystania z mediГіw.</li>
      <li><a href="/pl/archive?category=Konflikt">Konflikt</a> вЂ” Spory korporacyjne i gospodarcze w szczegГіЕ‚ach.</li>
      <li><a href="/pl/archive?category=Dokument">Dokument</a> вЂ” Analiza jednego konkretnego dokumentu z komentarzami.</li>
    </ul>
  </section>
  </Base>